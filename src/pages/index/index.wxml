<!--index.wxml-->
<map id="homeMap"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    controls="{{controls}}"
    markers="{{markers}}"
    bindcontroltap="controltap"
    bindmarkertap="markertap"
    bindregionchange="onRegionChange"
    bindtap="onMapTap"
    show-location />
