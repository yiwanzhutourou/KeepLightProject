<!--index.wxml-->
<map id="homeMap"
    longitude="{{longitude}}"
    latitude="{{latitude}}"
    markers="{{markers}}"
    bindcontroltap="controltap"
    bindmarkertap="markertap"
    show-location />
